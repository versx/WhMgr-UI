{{> header}}

{{> navbar}}

<br>
<div class="alert alert-danger w-75 {{^show_error}}d-none{{/show_error}}" style="float: none; margin: 0 auto;">
    <div id="error"><strong>Error!</strong> {{error}}</div>
</div>
{{#show_error}}<br>{{/show_error}}

<h1 align="center" id="header">{{Edit Pokemon Subscription}} {{id}}</h1>
<br>
<div class="w-75" style="float: none; margin: 0 auto;">
    <form action="/api/pokemon/edit/{{id}}" method="post">
        <div class="form-group">
            {{Form}}
            <input type="text" class="form-control" name="form" value="{{form}}" placeholder="i.e. Sunny">
        </div>
        <div class="form-group">
            {{IV}}
            <input type="text" class="form-control" name="iv" value="{{iv}}" placeholder="i.e. 100">
        </div>
        <div class="form-group">
            {{IV List}}
            <textarea class="form-control" name="iv_list" placeholder="i.e. 0/15/14&#10;0/15/15&#10;1/15/15" rows="5">{{iv_list}}</textarea>
        </div>
        <div class="row">
            <div class="col">
                <div class="form-group">
                    {{Minimum Level}}
                    <input type="text" class="form-control" name="min_lvl" value="{{min_lvl}}" placeholder="i.e. 0">
                </div>
            </div>
            <div class="col">
                <div class="form-group">
                    {{Maximum Level}}
                    <input type="text" class="form-control" name="max_lvl" value="{{max_lvl}}" placeholder="i.e. 35">
                </div>
            </div>
        </div>
        <div class="form-group">
            {{Gender}}
            <select name="gender" class="custom-select" required>
                {{#genders}}
                <option value="{{id}}" {{#selected}}selected{{/selected}}>{{name}}</option>
                {{/genders}}
            </select>
        </div>
        <div class="form-group">
            {{City}}
            <select name="city" class="custom-select" multiple required>
                <option value="all" >{{All}}</option>
                {{#cities}}
                <option value="{{name}}" id="g_{{guild}}" {{#selected}}selected{{/selected}} hidden>{{name}}</option>
                {{/cities}}
            </select>
        </div>
        <br>
        <input type="hidden" id="guild_id" name="guild_id">
        <button type="submit" class="btn btn-primary">{{Save}}</button>
        <br>
    </form>
    <form action="/api/pokemon/delete/{{id}}" method="post">
        <button type="submit" class="btn btn-danger" onclick="return confirm('{{Are you sure that you want to delete pokemon subscription}} {{id}}?');">{{Delete}}</button>
    </form>
</div>

<script type='text/javascript' src="/js/pokemon-list.js"></script>
<script type='text/javascript'>
    $('#guild_id').val(guildId);
    const guilds = document.querySelectorAll('*[id^="g_' + guildId + '"]');
    if (guilds) {
        guilds.forEach(guild => guild.hidden = false);
    }
    $('#server_selector').prop('disabled', true);
    console.log('server:', $('#server_selector').val());
</script>